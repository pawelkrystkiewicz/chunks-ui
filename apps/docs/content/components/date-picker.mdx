import { DatePickerBasicExample, DatePickerControlledExample, DatePickerConstrainedExample, DatePickerDisabledExample } from '@/examples/DatePicker'
import { generateDefinition, TSDoc } from 'nextra/tsdoc'

# DatePicker

A trigger-and-popover date picker built on Base UI's `Popover` primitive and the `Calendar` component. Selecting a date closes the popover automatically.

## Import

```tsx
import { DatePicker } from "chunks-ui";
```

## Basic Usage

```tsx
<DatePicker />
```

<DatePickerBasicExample />

## Controlled

```tsx
function ControlledDatePicker() {
  const [date, setDate] = useState<Date | null>(null);

  return (
    <DatePicker
      value={date}
      onValueChange={setDate}
      placeholder="Select a date"
    />
  );
}
```

<DatePickerControlledExample />

## Date Constraints

Use `min` and `max` to limit the selectable range. Dates outside the range are rendered disabled in the calendar:

```tsx
const today = new Date();
const nextMonth = new Date(today.getFullYear(), today.getMonth() + 1, today.getDate());

<DatePicker min={today} max={nextMonth} />
```

<DatePickerConstrainedExample />

## Disabled

```tsx
<DatePicker disabled />
```

<DatePickerDisabledExample />

## DatePicker Props

<TSDoc
  definition={generateDefinition({
    code: `
import type { DatePickerProps } from 'chunks-ui'
type Props = Pick<DatePickerProps, 'value' | 'defaultValue' | 'onValueChange' | 'placeholder' | 'disabled' | 'min' | 'max' | 'isDateDisabled'>
export default Props
`
  })}
/>
