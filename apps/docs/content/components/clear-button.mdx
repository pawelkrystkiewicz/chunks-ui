import { ClearButton } from 'chunks-ui'
import { Container } from '@/components'
import { ClearButtonInContextExample } from '@/examples/ClearButton'
import { generateDefinition, TSDoc } from 'nextra/tsdoc'

# ClearButton

A small circular icon button for clearing or dismissing values. Used internally by `Input` (via `onClear`) and `Chip` (via `onRemove`), but also exported for direct use.

## Import

```tsx
import { ClearButton } from 'chunks-ui'
```

## Basic Usage

```tsx
<ClearButton onClick={() => console.log('Cleared')} />
```

<Container>
  <ClearButton />
</Container>

## Custom Label

Use the `label` prop to override the default accessible label:

```tsx
<ClearButton label="Remove tag" />
```

<Container>
  <ClearButton label="Remove tag" />
</Container>

## Props

<TSDoc
  definition={generateDefinition({
    code: `
import type { ClearButtonProps } from 'chunks-ui'
type Props = Pick<ClearButtonProps, 'label'>
export default Props
`
  })}
/>

All other props are forwarded to the underlying `<button>` element.

## Styling

The button renders at `size-6` (24px) with a rounded-full shape. It shows `text-muted-foreground` by default and transitions to `bg-accent text-destructive` on hover.
